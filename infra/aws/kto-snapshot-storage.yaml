AWSTemplateFormatVersion: '2010-09-09'
Description: Private immutable-by-contract storage for KoReady KTO raw snapshots.

Parameters:
  EnvironmentName:
    Type: String
    Default: shared
    AllowedPattern: '[a-z0-9-]{2,20}'

Resources:
  SnapshotBucket:
    Type: AWS::S3::Bucket
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain
    Properties:
      BucketEncryption:
        ServerSideEncryptionConfiguration:
          - ServerSideEncryptionByDefault:
              SSEAlgorithm: AES256
      LifecycleConfiguration:
        Rules:
          - Id: AbortIncompleteMultipartUploads
            Status: Enabled
            AbortIncompleteMultipartUpload:
              DaysAfterInitiation: 7
      OwnershipControls:
        Rules:
          - ObjectOwnership: BucketOwnerEnforced
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        BlockPublicPolicy: true
        IgnorePublicAcls: true
        RestrictPublicBuckets: true
      VersioningConfiguration:
        Status: Enabled
      Tags:
        - Key: Project
          Value: KoReady
        - Key: Environment
          Value:
            Ref: EnvironmentName
        - Key: DataClassification
          Value: PrivateKtoSnapshot

  ProfileImageBucket:
    Type: AWS::S3::Bucket
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain
    Properties:
      BucketEncryption:
        ServerSideEncryptionConfiguration:
          - ServerSideEncryptionByDefault:
              SSEAlgorithm: AES256
      CorsConfiguration:
        CorsRules:
          - AllowedHeaders:
              - '*'
            AllowedMethods:
              - PUT
              - GET
              - HEAD
            AllowedOrigins:
              - '*'
            ExposedHeaders:
              - ETag
            MaxAge: 3000
      LifecycleConfiguration:
        Rules:
          - Id: AbortIncompleteMultipartUploads
            Status: Enabled
            AbortIncompleteMultipartUpload:
              DaysAfterInitiation: 1
          - Id: RemoveNoncurrentProfileImages
            Status: Enabled
            NoncurrentVersionExpiration:
              NoncurrentDays: 30
      OwnershipControls:
        Rules:
          - ObjectOwnership: BucketOwnerEnforced
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        BlockPublicPolicy: true
        IgnorePublicAcls: true
        RestrictPublicBuckets: true
      VersioningConfiguration:
        Status: Enabled
      Tags:
        - Key: Project
          Value: KoReady
        - Key: Environment
          Value:
            Ref: EnvironmentName
        - Key: DataClassification
          Value: PrivateUserProfileImage

  SnapshotWriterRole:
    Type: AWS::IAM::Role
    Properties:
      Description: KTO snapshot writes and Elastic Beanstalk web runtime permissions.
      MaxSessionDuration: 3600
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service:
                - ec2.amazonaws.com
            Action:
              - sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/AWSElasticBeanstalkWebTier
      Policies:
        - PolicyName: KtoSnapshotObjectAccess
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Sid: BucketLocation
                Effect: Allow
                Action:
                  - s3:GetBucketLocation
                Resource:
                  - Fn::GetAtt:
                      - SnapshotBucket
                      - Arn
                  - Fn::GetAtt:
                      - ProfileImageBucket
                      - Arn
              - Sid: SnapshotPrefix
                Effect: Allow
                Action:
                  - s3:ListBucket
                Resource:
                  Fn::GetAtt:
                    - SnapshotBucket
                    - Arn
                Condition:
                  StringLike:
                    s3:prefix:
                      - kto/*
              - Sid: SnapshotObjects
                Effect: Allow
                Action:
                  - s3:GetObject
                  - s3:PutObject
                Resource:
                  Fn::Sub: '${SnapshotBucket.Arn}/kto/*'
              - Sid: ProfileImageObjects
                Effect: Allow
                Action:
                  - s3:GetObject
                  - s3:PutObject
                  - s3:DeleteObject
                Resource:
                  Fn::Sub: '${ProfileImageBucket.Arn}/profile-images/*'

  SnapshotWriterInstanceProfile:
    Type: AWS::IAM::InstanceProfile
    Properties:
      Roles:
        - Ref: SnapshotWriterRole

  SnapshotBucketPolicy:
    Type: AWS::S3::BucketPolicy
    Properties:
      Bucket:
        Ref: SnapshotBucket
      PolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Sid: DenyInsecureTransport
            Effect: Deny
            Principal: '*'
            Action: s3:*
            Resource:
              - Fn::GetAtt:
                  - SnapshotBucket
                  - Arn
              - Fn::Sub: '${SnapshotBucket.Arn}/*'
            Condition:
              Bool:
                aws:SecureTransport: false
          - Sid: DenyUnconditionalSnapshotWrites
            Effect: Deny
            Principal: '*'
            Action: s3:PutObject
            Resource:
              Fn::Sub: '${SnapshotBucket.Arn}/kto/*'
            Condition:
              StringNotEquals:
                s3:if-none-match: '*'

  ProfileImageBucketPolicy:
    Type: AWS::S3::BucketPolicy
    Properties:
      Bucket:
        Ref: ProfileImageBucket
      PolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Sid: DenyInsecureTransport
            Effect: Deny
            Principal: '*'
            Action: s3:*
            Resource:
              - Fn::GetAtt:
                  - ProfileImageBucket
                  - Arn
              - Fn::Sub: '${ProfileImageBucket.Arn}/*'
            Condition:
              Bool:
                aws:SecureTransport: false

Outputs:
  SnapshotBucketName:
    Description: Private bucket for KTO gzip snapshots.
    Value:
      Ref: SnapshotBucket
  SnapshotWriterRoleArn:
    Description: IAM role ARN for a future EB EC2 instance profile.
    Value:
      Fn::GetAtt:
        - SnapshotWriterRole
        - Arn
  SnapshotWriterInstanceProfileName:
    Description: Instance profile name for a future EB environment.
    Value:
      Ref: SnapshotWriterInstanceProfile
  ProfileImageBucketName:
    Description: Private bucket for user profile images.
    Value:
      Ref: ProfileImageBucket
